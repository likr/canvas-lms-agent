// Auto-generated MCP Tools for Canvas LMS API
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const { genericHandler } = require("./helper");

const definitions = [
  {
    "name": "get_calendar_events",
    "description": "**Scope:** `url:GET|/api/v1/calendar_events` Retrieve the paginated list of calendar events or assignments for the current user Returns a list of [CalendarEvent](#calendarevent) objects. [CalendarEventsApiController#user\\_index](https://github.com/instructure/canvas-lms/blob/master/app/controllers/calendar_events_api_controller.rb)",
    "inputSchema": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "description": "Defaults to \"event\" Allowed values: `event`, `assignment`, `sub_assignment`"
        },
        "start_date": {
          "type": "string",
          "description": "<p>Only return events since the start\\_date (inclusive).<br>Defaults to today. The value should be formatted as: yyyy-mm-dd or ISO 8601 YYYY-MM-DDTHH:MM:SSZ.</p>"
        },
        "end_date": {
          "type": "string",
          "description": "<p>Only return events before the end\\_date (inclusive).<br>Defaults to start\\_date. The value should be formatted as: yyyy-mm-dd or ISO 8601 YYYY-MM-DDTHH:MM:SSZ.<br>If end\\_date is the same as start\\_date, then only events on that day are<br>returned.</p>"
        },
        "undated": {
          "type": "boolean",
          "description": "<p>Defaults to false (dated events only).<br>If true, only return undated events and ignore start\\_date and end\\_date.</p>"
        },
        "all_events": {
          "type": "boolean",
          "description": "<p>Defaults to false (uses start\\_date, end\\_date, and undated criteria).<br>If true, all events are returned, ignoring start\\_date, end\\_date, and undated criteria.</p>"
        },
        "context_codes[]": {
          "type": "string",
          "description": "<p>List of context codes of courses, groups, users, or accounts whose events you want to see.<br>If not specified, defaults to the current user (i.e personal calendar,<br>no course/group events). Limited to 10 context codes, additional ones are<br>ignored. The format of this field is the context type, followed by an<br>underscore, followed by the context id. For example: course\\_42</p>"
        },
        "excludes[]": {
          "type": "array",
          "description": "Array of attributes to exclude. Possible values are \"description\", \"child\\_events\" and \"assignment\""
        },
        "includes[]": {
          "type": "array",
          "description": "Array of optional attributes to include. Possible values are \"web\\_conference\" and \"series\\_natural\\_language\""
        },
        "important_dates": {
          "type": "boolean",
          "description": "<p>Defaults to false.<br>If true, only events with important dates set to true will be returned.</p>"
        },
        "blackout_date": {
          "type": "boolean",
          "description": "<p>Defaults to false.<br>If true, only events with blackout date set to true will be returned.</p>"
        },
        "fetch_all_pages": {
          "type": "boolean",
          "description": "Optional: Set to true to automatically paginate and return all pages of results. Default is false."
        }
      }
    }
  },
  {
    "name": "get_users_user_id_calendar_events",
    "description": "**Scope:** `url:GET|/api/v1/users/:user_id/calendar_events` Retrieve the paginated list of calendar events or assignments for the specified user. To view calendar events for a user other than yourself, you must either be an observer of that user or an administrator. Returns a list of [CalendarEvent](#calendarevent) objects. [CalendarEventsApiController#create](https://github.com/instructure/canvas-lms/blob/master/app/controllers/calendar_events_api_controller.rb)",
    "inputSchema": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "Path parameter: user_id"
        },
        "type": {
          "type": "string",
          "description": "Defaults to \"event\" Allowed values: `event`, `assignment`"
        },
        "start_date": {
          "type": "string",
          "description": "<p>Only return events since the start\\_date (inclusive).<br>Defaults to today. The value should be formatted as: yyyy-mm-dd or ISO 8601 YYYY-MM-DDTHH:MM:SSZ.</p>"
        },
        "end_date": {
          "type": "string",
          "description": "<p>Only return events before the end\\_date (inclusive).<br>Defaults to start\\_date. The value should be formatted as: yyyy-mm-dd or ISO 8601 YYYY-MM-DDTHH:MM:SSZ.<br>If end\\_date is the same as start\\_date, then only events on that day are<br>returned.</p>"
        },
        "undated": {
          "type": "boolean",
          "description": "<p>Defaults to false (dated events only).<br>If true, only return undated events and ignore start\\_date and end\\_date.</p>"
        },
        "all_events": {
          "type": "boolean",
          "description": "<p>Defaults to false (uses start\\_date, end\\_date, and undated criteria).<br>If true, all events are returned, ignoring start\\_date, end\\_date, and undated criteria.</p>"
        },
        "context_codes[]": {
          "type": "string",
          "description": "<p>List of context codes of courses, groups, users, or accounts whose events you want to see.<br>If not specified, defaults to the current user (i.e personal calendar,<br>no course/group events). Limited to 10 context codes, additional ones are<br>ignored. The format of this field is the context type, followed by an<br>underscore, followed by the context id. For example: course\\_42</p>"
        },
        "excludes[]": {
          "type": "array",
          "description": "Array of attributes to exclude. Possible values are \"description\", \"child\\_events\" and \"assignment\""
        },
        "submission_types[]": {
          "type": "array",
          "description": "<p>When type is \"assignment\", specifies the allowable submission types for returned assignments.<br>Ignored if type is not \"assignment\" or if exclude\\_submission\\_types is provided.</p>"
        },
        "exclude_submission_types[]": {
          "type": "array",
          "description": "<p>When type is \"assignment\", specifies the submission types to be excluded from the returned<br>assignments. Ignored if type is not \"assignment\".</p>"
        },
        "includes[]": {
          "type": "array",
          "description": "Array of optional attributes to include. Possible values are \"web\\_conference\" and \"series\\_natural\\_language\""
        },
        "important_dates": {
          "type": "boolean",
          "description": "<p>Defaults to false<br>If true, only events with important dates set to true will be returned.</p>"
        },
        "blackout_date": {
          "type": "boolean",
          "description": "<p>Defaults to false<br>If true, only events with blackout date set to true will be returned.</p>"
        },
        "fetch_all_pages": {
          "type": "boolean",
          "description": "Optional: Set to true to automatically paginate and return all pages of results. Default is false."
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "post_calendar_events",
    "description": "**Scope:** `url:POST|/api/v1/calendar_events` Create and return a new calendar event ```bash curl 'https:///api/v1/calendar_events.json' \\ -X POST \\ -F 'calendar_event[context_code]=course_123' \\ -F 'calendar_event[title]=Paintball Fight!' \\ -F 'calendar_event[start_at]=2012-07-19T21:00:00Z' \\ -F 'calendar_event[end_at]=2012-07-19T22:00:00Z' \\ -H \"Authorization: Bearer \" ``` [CalendarEventsApiController#show](https://github.com/instructure/canvas-lms/blob/master/app/controllers/calendar_event...",
    "inputSchema": {
      "type": "object",
      "properties": {
        "calendar_event[context_code]": {
          "type": "string",
          "description": "<p>Context code of the course, group, user, or account whose calendar<br>this event should be added to.</p>"
        },
        "calendar_event[title]": {
          "type": "string",
          "description": "Short title for the calendar event."
        },
        "calendar_event[description]": {
          "type": "string",
          "description": "Longer HTML description of the event."
        },
        "calendar_event[start_at]": {
          "type": "string",
          "description": "Start date/time of the event."
        },
        "calendar_event[end_at]": {
          "type": "string",
          "description": "End date/time of the event."
        },
        "calendar_event[location_name]": {
          "type": "string",
          "description": "Location name of the event."
        },
        "calendar_event[location_address]": {
          "type": "string",
          "description": "Location address"
        },
        "calendar_event[time_zone_edited]": {
          "type": "string",
          "description": "<p>Time zone of the user editing the event. Allowed time zones are<br><a href=\"http://www.iana.org/time-zones\">IANA time zones</a> or friendlier<br><a href=\"http://api.rubyonrails.org/classes/ActiveSupport/TimeZone.html\">Ruby on Rails time zones</a>.</p>"
        },
        "calendar_event[all_day]": {
          "type": "boolean",
          "description": "When true event is considered to span the whole day and times are ignored."
        },
        "calendar_event[child_event_data][X][start_at]": {
          "type": "string",
          "description": "<p>Section-level start time(s) if this is a course event. X can be any<br>identifier, provided that it is consistent across the start\\_at, end\\_at<br>and context\\_code</p>"
        },
        "calendar_event[child_event_data][X][end_at]": {
          "type": "string",
          "description": "Section-level end time(s) if this is a course event."
        },
        "calendar_event[child_event_data][X][context_code]": {
          "type": "string",
          "description": "Context code(s) corresponding to the section-level start and end time(s)."
        },
        "calendar_event[duplicate][count]": {
          "type": "number",
          "description": "Number of times to copy/duplicate the event. Count cannot exceed 200."
        },
        "calendar_event[duplicate][interval]": {
          "type": "number",
          "description": "Defaults to 1 if duplicate `count` is set. The interval between the duplicated events."
        },
        "calendar_event[duplicate][frequency]": {
          "type": "string",
          "description": "Defaults to \"weekly\". The frequency at which to duplicate the event Allowed values: `daily`, `weekly`, `monthly`"
        },
        "calendar_event[duplicate][append_iterator]": {
          "type": "boolean",
          "description": "<p>Defaults to false. If set to <code>true</code>, an increasing counter number will be appended to the event title<br>when the event is duplicated. (e.g. Event 1, Event 2, Event 3, etc)</p>"
        },
        "calendar_event[rrule]": {
          "type": "string",
          "description": "<p>The recurrence rule to create a series of recurring events.<br>Its value is the <a href=\"https://icalendar.org/iCalendar-RFC-5545/3-8-5-3-recurrence-rule.html\">iCalendar RRULE</a><br>defining how the event repeats. Unending series not supported.</p>"
        },
        "calendar_event[blackout_date]": {
          "type": "boolean",
          "description": "<p>If the blackout\\_date is true, this event represents a holiday or some<br>other special day that does not count in course pacing.</p>"
        }
      },
      "required": [
        "calendar_event[context_code]"
      ]
    }
  },
  {
    "name": "get_calendar_events_id",
    "description": "**Scope:** `url:GET|/api/v1/calendar_events/:id` Returns detailed information about a specific calendar event or assignment. Returns a [CalendarEvent](#calendarevent) object. [CalendarEventsApiController#reserve](https://github.com/instructure/canvas-lms/blob/master/app/controllers/calendar_events_api_controller.rb)",
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Path parameter: id"
        },
        "fetch_all_pages": {
          "type": "boolean",
          "description": "Optional: Set to true to automatically paginate and return all pages of results. Default is false."
        }
      },
      "required": [
        "id"
      ]
    }
  },
  {
    "name": "post_calendar_events_id_reservations",
    "description": "**Scope:** `url:POST|/api/v1/calendar_events/:id/reservations`",
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Path parameter: id"
        }
      },
      "required": [
        "id"
      ]
    }
  },
  {
    "name": "post_calendar_events_id_reservations_participant_id",
    "description": "**Scope:** `url:POST|/api/v1/calendar_events/:id/reservations/:participant_id` Reserves a particular time slot and return the new reservation ```bash curl 'https:///api/v1/calendar_events/345/reservations.json' \\ -X POST \\ -F 'cancel_existing=true' \\ -H \"Authorization: Bearer \" ``` [CalendarEventsApiController#update](https://github.com/instructure/canvas-lms/blob/master/app/controllers/calendar_events_api_controller.rb)",
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Path parameter: id"
        },
        "participant_id": {
          "type": "string",
          "description": "<p>User or group id for whom you are making the reservation (depends on the<br>participant type). Defaults to the current user (or user's candidate group).</p>"
        },
        "comments": {
          "type": "string",
          "description": "Comments to associate with this reservation"
        },
        "cancel_existing": {
          "type": "boolean",
          "description": "<p>Defaults to false. If true, cancel any previous reservation(s) for this<br>participant and appointment group.</p>"
        }
      },
      "required": [
        "id",
        "participant_id"
      ]
    }
  },
  {
    "name": "put_calendar_events_id",
    "description": "**Scope:** `url:PUT|/api/v1/calendar_events/:id` Update and return a calendar event ```bash curl 'https:///api/v1/calendar_events/234' \\ -X PUT \\ -F 'calendar_event[title]=Epic Paintball Fight!' \\ -H \"Authorization: Bearer \" ``` [CalendarEventsApiController#destroy](https://github.com/instructure/canvas-lms/blob/master/app/controllers/calendar_events_api_controller.rb)",
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Path parameter: id"
        },
        "calendar_event[context_code]": {
          "type": "string",
          "description": "<p>Context code of the course, group, user, or account to move this event to.<br>Scheduler appointments and events with section-specific times cannot be moved between calendars.</p>"
        },
        "calendar_event[title]": {
          "type": "string",
          "description": "Short title for the calendar event."
        },
        "calendar_event[description]": {
          "type": "string",
          "description": "Longer HTML description of the event."
        },
        "calendar_event[start_at]": {
          "type": "string",
          "description": "Start date/time of the event."
        },
        "calendar_event[end_at]": {
          "type": "string",
          "description": "End date/time of the event."
        },
        "calendar_event[location_name]": {
          "type": "string",
          "description": "Location name of the event."
        },
        "calendar_event[location_address]": {
          "type": "string",
          "description": "Location address"
        },
        "calendar_event[time_zone_edited]": {
          "type": "string",
          "description": "<p>Time zone of the user editing the event. Allowed time zones are<br><a href=\"http://www.iana.org/time-zones\">IANA time zones</a> or friendlier<br><a href=\"http://api.rubyonrails.org/classes/ActiveSupport/TimeZone.html\">Ruby on Rails time zones</a>.</p>"
        },
        "calendar_event[all_day]": {
          "type": "boolean",
          "description": "When true event is considered to span the whole day and times are ignored."
        },
        "calendar_event[child_event_data][X][start_at]": {
          "type": "string",
          "description": "<p>Section-level start time(s) if this is a course event. X can be any<br>identifier, provided that it is consistent across the start\\_at, end\\_at<br>and context\\_code</p>"
        },
        "calendar_event[child_event_data][X][end_at]": {
          "type": "string",
          "description": "Section-level end time(s) if this is a course event."
        },
        "calendar_event[child_event_data][X][context_code]": {
          "type": "string",
          "description": "Context code(s) corresponding to the section-level start and end time(s)."
        },
        "calendar_event[rrule]": {
          "type": "string",
          "description": "<p>Valid if the event whose ID is in the URL is part of a series.<br>This defines the shape of the recurring event series after it's updated.<br>Its value is the iCalendar RRULE. Unending series are not supported.</p>"
        },
        "which": {
          "type": "string",
          "description": "<p>Valid if the event whose ID is in the URL is part of a series.<br>Update just the event whose ID is in in the URL, all events<br>in the series, or the given event and all those following.<br>Some updates may create a new series. For example, changing the start time<br>of this and all following events from the middle of a series. Allowed values: <code>one</code>, <code>all</code>, <code>following</code></p>"
        },
        "calendar_event[blackout_date]": {
          "type": "boolean",
          "description": "<p>If the blackout\\_date is true, this event represents a holiday or some<br>other special day that does not count in course pacing.</p>"
        }
      },
      "required": [
        "id"
      ]
    }
  },
  {
    "name": "delete_calendar_events_id",
    "description": "**Scope:** `url:DELETE|/api/v1/calendar_events/:id` Delete an event from the calendar and return the deleted event ```bash curl 'https:///api/v1/calendar_events/234' \\ -X DELETE \\ -F 'cancel_reason=Greendale layed off the janitorial staff :(' \\ -F 'which=following' -H \"Authorization: Bearer \" ``` [CalendarEventsApiController#save\\_enabled\\_account\\_calendars](https://github.com/instructure/canvas-lms/blob/master/app/controllers/calendar_events_api_controller.rb)",
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Path parameter: id"
        },
        "cancel_reason": {
          "type": "string",
          "description": "Reason for deleting/canceling the event."
        },
        "which": {
          "type": "string",
          "description": "<p>Valid if the event whose ID is in the URL is part of a series.<br>Delete just the event whose ID is in in the URL, all events<br>in the series, or the given event and all those following. Allowed values: <code>one</code>, <code>all</code>, <code>following</code></p>"
        }
      },
      "required": [
        "id"
      ]
    }
  },
  {
    "name": "post_calendar_events_save_enabled_account_calendars",
    "description": "**Scope:** `url:POST|/api/v1/calendar_events/save_enabled_account_calendars` Creates and updates the enabled\\_account\\_calendars and mark\\_feature\\_as\\_seen user preferences ```bash curl 'https:///api/v1/calendar_events/save_enabled_account_calendars' \\ -X POST \\ -F 'mark_feature_as_seen=true' \\ -F 'enabled_account_calendars[]=1' \\ -F 'enabled_account_calendars[]=2' \\ -H \"Authorization: Bearer \" ``` [CalendarEventsApiController#set\\_course\\_timetable](https://github.com/instructure/canvas-lms...",
    "inputSchema": {
      "type": "object",
      "properties": {
        "mark_feature_as_seen": {
          "type": "boolean",
          "description": "Flag to mark account calendars feature as seen"
        },
        "enabled_account_calendars[]": {
          "type": "array",
          "description": "An array of account Ids to remember in the calendars list of the user"
        }
      }
    }
  },
  {
    "name": "post_courses_course_id_calendar_events_timetable",
    "description": "**Scope:** `url:POST|/api/v1/courses/:course_id/calendar_events/timetable` Creates and updates \"timetable\" events for a course. Can automaticaly generate a series of calendar events based on simple schedules (e.g. \"Monday and Wednesday at 2:00pm\" ) Existing timetable events for the course and course sections will be updated if they still are part of the timetable. Otherwise, they will be deleted. ```bash curl 'https:///api/v1/calendar_events/timetable' \\ -X POST \\ -F 'timetables[all][][weekda...",
    "inputSchema": {
      "type": "object",
      "properties": {
        "course_id": {
          "type": "string",
          "description": "Path parameter: course_id"
        },
        "timetables[course_section_id][]": {
          "type": "array",
          "description": "<p>An array of timetable objects for the course section specified by course\\_section\\_id.<br>If course\\_section\\_id is set to \"all\", events will be created for the entire course.</p>"
        },
        "timetables[course_section_id][][weekdays]": {
          "type": "string",
          "description": "<p>A comma-separated list of abbreviated weekdays<br>(Mon-Monday, Tue-Tuesday, Wed-Wednesday, Thu-Thursday, Fri-Friday, Sat-Saturday, Sun-Sunday)</p>"
        },
        "timetables[course_section_id][][start_time]": {
          "type": "string",
          "description": "Time to start each event at (e.g. \"9:00 am\")"
        },
        "timetables[course_section_id][][end_time]": {
          "type": "string",
          "description": "Time to end each event at (e.g. \"9:00 am\")"
        },
        "timetables[course_section_id][][location_name]": {
          "type": "string",
          "description": "A location name to set for each event"
        }
      },
      "required": [
        "course_id"
      ]
    }
  },
  {
    "name": "get_courses_course_id_calendar_events_timetable",
    "description": "**Scope:** `url:GET|/api/v1/courses/:course_id/calendar_events/timetable` Returns the last timetable set by the [Set a course timetable](#method.calendar_events_api.set_course_timetable) endpoint [CalendarEventsApiController#set\\_course\\_timetable\\_events](https://github.com/instructure/canvas-lms/blob/master/app/controllers/calendar_events_api_controller.rb)",
    "inputSchema": {
      "type": "object",
      "properties": {
        "course_id": {
          "type": "string",
          "description": "Path parameter: course_id"
        },
        "fetch_all_pages": {
          "type": "boolean",
          "description": "Optional: Set to true to automatically paginate and return all pages of results. Default is false."
        }
      },
      "required": [
        "course_id"
      ]
    }
  },
  {
    "name": "post_courses_course_id_calendar_events_timetable_events",
    "description": "**Scope:** `url:POST|/api/v1/courses/:course_id/calendar_events/timetable_events` Creates and updates \"timetable\" events for a course or course section. Similar to [setting a course timetable](#method.calendar_events_api.set_course_timetable), but instead of generating a list of events based on a timetable schedule, this endpoint expects a complete list of events. *** This documentation is generated directly from the Canvas LMS source code, available [on Github](https://github.com/instructure...",
    "inputSchema": {
      "type": "object",
      "properties": {
        "course_id": {
          "type": "string",
          "description": "Path parameter: course_id"
        },
        "course_section_id": {
          "type": "string",
          "description": "<p>Events will be created for the course section specified by course\\_section\\_id.<br>If not present, events will be created for the entire course.</p>"
        },
        "events[]": {
          "type": "array",
          "description": "An array of event objects to use."
        },
        "events[][start_at]": {
          "type": "string",
          "description": "Start time for the event"
        },
        "events[][end_at]": {
          "type": "string",
          "description": "End time for the event"
        },
        "events[][location_name]": {
          "type": "string",
          "description": "Location name for the event"
        },
        "events[][code]": {
          "type": "string",
          "description": "<p>A unique identifier that can be used to update the event at a later time<br>If one is not specified, an identifier will be generated based on the start and end times</p>"
        },
        "events[][title]": {
          "type": "string",
          "description": "Title for the meeting. If not present, will default to the associated course's name"
        }
      },
      "required": [
        "course_id"
      ]
    }
  }
];

const handlers = {
  get_calendar_events: async (client, args) => {
    return genericHandler(client, "GET", "/api/v1/calendar_events", args);
  },
  get_users_user_id_calendar_events: async (client, args) => {
    return genericHandler(client, "GET", "/api/v1/users/:user_id/calendar_events", args);
  },
  post_calendar_events: async (client, args) => {
    return genericHandler(client, "POST", "/api/v1/calendar_events", args);
  },
  get_calendar_events_id: async (client, args) => {
    return genericHandler(client, "GET", "/api/v1/calendar_events/:id", args);
  },
  post_calendar_events_id_reservations: async (client, args) => {
    return genericHandler(client, "POST", "/api/v1/calendar_events/:id/reservations", args);
  },
  post_calendar_events_id_reservations_participant_id: async (client, args) => {
    return genericHandler(client, "POST", "/api/v1/calendar_events/:id/reservations/:participant_id", args);
  },
  put_calendar_events_id: async (client, args) => {
    return genericHandler(client, "PUT", "/api/v1/calendar_events/:id", args);
  },
  delete_calendar_events_id: async (client, args) => {
    return genericHandler(client, "DELETE", "/api/v1/calendar_events/:id", args);
  },
  post_calendar_events_save_enabled_account_calendars: async (client, args) => {
    return genericHandler(client, "POST", "/api/v1/calendar_events/save_enabled_account_calendars", args);
  },
  post_courses_course_id_calendar_events_timetable: async (client, args) => {
    return genericHandler(client, "POST", "/api/v1/courses/:course_id/calendar_events/timetable", args);
  },
  get_courses_course_id_calendar_events_timetable: async (client, args) => {
    return genericHandler(client, "GET", "/api/v1/courses/:course_id/calendar_events/timetable", args);
  },
  post_courses_course_id_calendar_events_timetable_events: async (client, args) => {
    return genericHandler(client, "POST", "/api/v1/courses/:course_id/calendar_events/timetable_events", args);
  }
};

module.exports = {
  definitions,
  handlers
};
