// Auto-generated tests for shared_brand_configs
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const test = require("node:test");
const assert = require("node:assert");
const shared_brand_configsModule = require("../tools/shared_brand_configs");

test("post_accounts_account_id_shared_brand_configs calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = shared_brand_configsModule.handlers.post_accounts_account_id_shared_brand_configs;
  assert.ok(handler, "Handler post_accounts_account_id_shared_brand_configs should be defined");

  const result = await handler(mockClient, {"account_id": "test_account_id", "shared_brand_config[name]": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/accounts/test_account_id/shared_brand_configs");
  assert.deepStrictEqual(calledConfig.data, {"shared_brand_config[name]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("put_accounts_account_id_shared_brand_configs_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = shared_brand_configsModule.handlers.put_accounts_account_id_shared_brand_configs_id;
  assert.ok(handler, "Handler put_accounts_account_id_shared_brand_configs_id should be defined");

  const result = await handler(mockClient, {"account_id": "test_account_id", "id": "test_id"});

  assert.strictEqual(calledConfig.method, "put");
  assert.strictEqual(calledConfig.url, "/api/v1/accounts/test_account_id/shared_brand_configs/test_id");
  assert.deepStrictEqual(calledConfig.data, {});
  assert.deepStrictEqual(result, { success: true });
});

test("delete_shared_brand_configs_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = shared_brand_configsModule.handlers.delete_shared_brand_configs_id;
  assert.ok(handler, "Handler delete_shared_brand_configs_id should be defined");

  const result = await handler(mockClient, {"id": "test_id"});

  assert.strictEqual(calledConfig.method, "delete");
  assert.strictEqual(calledConfig.url, "/api/v1/shared_brand_configs/test_id");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});
