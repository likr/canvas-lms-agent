// Auto-generated tests for account_calendars
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const test = require("node:test");
const assert = require("node:assert");
const account_calendarsModule = require("../tools/account_calendars");

test("get_account_calendars calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = account_calendarsModule.handlers.get_account_calendars;
  assert.ok(handler, "Handler get_account_calendars should be defined");

  const result = await handler(mockClient, {"search_term": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/account_calendars");
  assert.deepStrictEqual(calledConfig.params, {"search_term": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("get_account_calendars_account_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = account_calendarsModule.handlers.get_account_calendars_account_id;
  assert.ok(handler, "Handler get_account_calendars_account_id should be defined");

  const result = await handler(mockClient, {"account_id": "test_account_id"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/account_calendars/test_account_id");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});

test("put_account_calendars_account_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = account_calendarsModule.handlers.put_account_calendars_account_id;
  assert.ok(handler, "Handler put_account_calendars_account_id should be defined");

  const result = await handler(mockClient, {"account_id": "test_account_id", "visible": true});

  assert.strictEqual(calledConfig.method, "put");
  assert.strictEqual(calledConfig.url, "/api/v1/account_calendars/test_account_id");
  assert.deepStrictEqual(calledConfig.data, {"visible": true});
  assert.deepStrictEqual(result, { success: true });
});

test("put_accounts_account_id_account_calendars calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = account_calendarsModule.handlers.put_accounts_account_id_account_calendars;
  assert.ok(handler, "Handler put_accounts_account_id_account_calendars should be defined");

  const result = await handler(mockClient, {"account_id": "test_account_id"});

  assert.strictEqual(calledConfig.method, "put");
  assert.strictEqual(calledConfig.url, "/api/v1/accounts/test_account_id/account_calendars");
  assert.deepStrictEqual(calledConfig.data, {});
  assert.deepStrictEqual(result, { success: true });
});

test("get_accounts_account_id_account_calendars calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = account_calendarsModule.handlers.get_accounts_account_id_account_calendars;
  assert.ok(handler, "Handler get_accounts_account_id_account_calendars should be defined");

  const result = await handler(mockClient, {"account_id": "test_account_id", "search_term": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/accounts/test_account_id/account_calendars");
  assert.deepStrictEqual(calledConfig.params, {"search_term": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("get_accounts_account_id_visible_calendars_count calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = account_calendarsModule.handlers.get_accounts_account_id_visible_calendars_count;
  assert.ok(handler, "Handler get_accounts_account_id_visible_calendars_count should be defined");

  const result = await handler(mockClient, {"account_id": "test_account_id"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/accounts/test_account_id/visible_calendars_count");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});
