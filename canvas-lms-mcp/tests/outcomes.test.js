// Auto-generated tests for outcomes
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const test = require("node:test");
const assert = require("node:assert");
const outcomesModule = require("../tools/outcomes");

test("get_outcomes_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = outcomesModule.handlers.get_outcomes_id;
  assert.ok(handler, "Handler get_outcomes_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "add_defaults": true});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/outcomes/test_id");
  assert.deepStrictEqual(calledConfig.params, {"add_defaults": true});
  assert.deepStrictEqual(result, { success: true });
});

test("put_outcomes_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = outcomesModule.handlers.put_outcomes_id;
  assert.ok(handler, "Handler put_outcomes_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "title": "test_val"});

  assert.strictEqual(calledConfig.method, "put");
  assert.strictEqual(calledConfig.url, "/api/v1/outcomes/test_id");
  assert.deepStrictEqual(calledConfig.data, {"title": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("get_courses_course_id_outcome_alignments calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = outcomesModule.handlers.get_courses_course_id_outcome_alignments;
  assert.ok(handler, "Handler get_courses_course_id_outcome_alignments should be defined");

  const result = await handler(mockClient, {"course_id": "test_course_id", "student_id": 123});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/courses/test_course_id/outcome_alignments");
  assert.deepStrictEqual(calledConfig.params, {"student_id": 123});
  assert.deepStrictEqual(result, { success: true });
});
