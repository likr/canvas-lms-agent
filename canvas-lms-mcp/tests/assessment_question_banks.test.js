// Auto-generated tests for assessment_question_banks
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const test = require("node:test");
const assert = require("node:assert");
const assessment_question_banksModule = require("../tools/assessment_question_banks");

test("get_question_banks calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = assessment_question_banksModule.handlers.get_question_banks;
  assert.ok(handler, "Handler get_question_banks should be defined");

  const result = await handler(mockClient, {"context_type": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/question_banks");
  assert.deepStrictEqual(calledConfig.params, {"context_type": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("get_question_banks_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = assessment_question_banksModule.handlers.get_question_banks_id;
  assert.ok(handler, "Handler get_question_banks_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "include_question_count": true});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/question_banks/test_id");
  assert.deepStrictEqual(calledConfig.params, {"include_question_count": true});
  assert.deepStrictEqual(result, { success: true });
});

test("get_question_banks_id_questions calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = assessment_question_banksModule.handlers.get_question_banks_id_questions;
  assert.ok(handler, "Handler get_question_banks_id_questions should be defined");

  const result = await handler(mockClient, {"id": "test_id"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/question_banks/test_id/questions");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});
