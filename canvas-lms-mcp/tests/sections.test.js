// Auto-generated tests for sections
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const test = require("node:test");
const assert = require("node:assert");
const sectionsModule = require("../tools/sections");

test("get_courses_course_id_sections calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = sectionsModule.handlers.get_courses_course_id_sections;
  assert.ok(handler, "Handler get_courses_course_id_sections should be defined");

  const result = await handler(mockClient, {"course_id": "test_course_id", "include[]": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/courses/test_course_id/sections");
  assert.deepStrictEqual(calledConfig.params, {"include[]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("post_courses_course_id_sections calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = sectionsModule.handlers.post_courses_course_id_sections;
  assert.ok(handler, "Handler post_courses_course_id_sections should be defined");

  const result = await handler(mockClient, {"course_id": "test_course_id", "course_section[name]": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/courses/test_course_id/sections");
  assert.deepStrictEqual(calledConfig.data, {"course_section[name]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("post_sections_id_crosslist_new_course_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = sectionsModule.handlers.post_sections_id_crosslist_new_course_id;
  assert.ok(handler, "Handler post_sections_id_crosslist_new_course_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "new_course_id": "test_new_course_id", "override_sis_stickiness": true});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/sections/test_id/crosslist/test_new_course_id");
  assert.deepStrictEqual(calledConfig.data, {"override_sis_stickiness": true});
  assert.deepStrictEqual(result, { success: true });
});

test("delete_sections_id_crosslist calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = sectionsModule.handlers.delete_sections_id_crosslist;
  assert.ok(handler, "Handler delete_sections_id_crosslist should be defined");

  const result = await handler(mockClient, {"id": "test_id", "override_sis_stickiness": true});

  assert.strictEqual(calledConfig.method, "delete");
  assert.strictEqual(calledConfig.url, "/api/v1/sections/test_id/crosslist");
  assert.deepStrictEqual(calledConfig.params, {"override_sis_stickiness": true});
  assert.deepStrictEqual(result, { success: true });
});

test("put_sections_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = sectionsModule.handlers.put_sections_id;
  assert.ok(handler, "Handler put_sections_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "course_section[name]": "test_val"});

  assert.strictEqual(calledConfig.method, "put");
  assert.strictEqual(calledConfig.url, "/api/v1/sections/test_id");
  assert.deepStrictEqual(calledConfig.data, {"course_section[name]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("get_courses_course_id_sections_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = sectionsModule.handlers.get_courses_course_id_sections_id;
  assert.ok(handler, "Handler get_courses_course_id_sections_id should be defined");

  const result = await handler(mockClient, {"course_id": "test_course_id", "id": "test_id"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/courses/test_course_id/sections/test_id");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});

test("get_sections_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = sectionsModule.handlers.get_sections_id;
  assert.ok(handler, "Handler get_sections_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "include[]": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/sections/test_id");
  assert.deepStrictEqual(calledConfig.params, {"include[]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("delete_sections_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = sectionsModule.handlers.delete_sections_id;
  assert.ok(handler, "Handler delete_sections_id should be defined");

  const result = await handler(mockClient, {"id": "test_id"});

  assert.strictEqual(calledConfig.method, "delete");
  assert.strictEqual(calledConfig.url, "/api/v1/sections/test_id");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});

test("get_sections_id_users calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = sectionsModule.handlers.get_sections_id_users;
  assert.ok(handler, "Handler get_sections_id_users should be defined");

  const result = await handler(mockClient, {"id": "test_id", "search_term": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/sections/test_id/users");
  assert.deepStrictEqual(calledConfig.params, {"search_term": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});
