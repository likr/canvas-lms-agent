// Auto-generated tests for polls
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const test = require("node:test");
const assert = require("node:assert");
const pollsModule = require("../tools/polls");

test("get_polls calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = pollsModule.handlers.get_polls;
  assert.ok(handler, "Handler get_polls should be defined");

  const result = await handler(mockClient, {});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/polls");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});

test("get_polls_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = pollsModule.handlers.get_polls_id;
  assert.ok(handler, "Handler get_polls_id should be defined");

  const result = await handler(mockClient, {"id": "test_id"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/polls/test_id");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});

test("post_polls calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = pollsModule.handlers.post_polls;
  assert.ok(handler, "Handler post_polls should be defined");

  const result = await handler(mockClient, {"polls[][question]": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/polls");
  assert.deepStrictEqual(calledConfig.data, {"polls[][question]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("put_polls_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = pollsModule.handlers.put_polls_id;
  assert.ok(handler, "Handler put_polls_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "polls[][question]": "test_val"});

  assert.strictEqual(calledConfig.method, "put");
  assert.strictEqual(calledConfig.url, "/api/v1/polls/test_id");
  assert.deepStrictEqual(calledConfig.data, {"polls[][question]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("delete_polls_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = pollsModule.handlers.delete_polls_id;
  assert.ok(handler, "Handler delete_polls_id should be defined");

  const result = await handler(mockClient, {"id": "test_id"});

  assert.strictEqual(calledConfig.method, "delete");
  assert.strictEqual(calledConfig.url, "/api/v1/polls/test_id");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});
