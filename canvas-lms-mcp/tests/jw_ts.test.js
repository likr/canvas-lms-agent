// Auto-generated tests for jw_ts
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const test = require("node:test");
const assert = require("node:assert");
const jw_tsModule = require("../tools/jw_ts");

test("post_jwts calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = jw_tsModule.handlers.post_jwts;
  assert.ok(handler, "Handler post_jwts should be defined");

  const result = await handler(mockClient, {"workflows[]": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/jwts");
  assert.deepStrictEqual(calledConfig.data, {"workflows[]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("post_jwts_refresh calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = jw_tsModule.handlers.post_jwts_refresh;
  assert.ok(handler, "Handler post_jwts_refresh should be defined");

  const result = await handler(mockClient, {"jwt": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/jwts/refresh");
  assert.deepStrictEqual(calledConfig.data, {"jwt": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});
