// Auto-generated tests for developer_keys
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const test = require("node:test");
const assert = require("node:assert");
const developer_keysModule = require("../tools/developer_keys");

test("get_accounts_account_id_developer_keys calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = developer_keysModule.handlers.get_accounts_account_id_developer_keys;
  assert.ok(handler, "Handler get_accounts_account_id_developer_keys should be defined");

  const result = await handler(mockClient, {"account_id": "test_account_id", "inherited": true});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/accounts/test_account_id/developer_keys");
  assert.deepStrictEqual(calledConfig.params, {"inherited": true});
  assert.deepStrictEqual(result, { success: true });
});

test("post_accounts_account_id_developer_keys calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = developer_keysModule.handlers.post_accounts_account_id_developer_keys;
  assert.ok(handler, "Handler post_accounts_account_id_developer_keys should be defined");

  const result = await handler(mockClient, {"account_id": "test_account_id", "developer_key": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/accounts/test_account_id/developer_keys");
  assert.deepStrictEqual(calledConfig.data, {"developer_key": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("put_developer_keys_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = developer_keysModule.handlers.put_developer_keys_id;
  assert.ok(handler, "Handler put_developer_keys_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "developer_key": "test_val"});

  assert.strictEqual(calledConfig.method, "put");
  assert.strictEqual(calledConfig.url, "/api/v1/developer_keys/test_id");
  assert.deepStrictEqual(calledConfig.data, {"developer_key": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("delete_developer_keys_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = developer_keysModule.handlers.delete_developer_keys_id;
  assert.ok(handler, "Handler delete_developer_keys_id should be defined");

  const result = await handler(mockClient, {"id": "test_id"});

  assert.strictEqual(calledConfig.method, "delete");
  assert.strictEqual(calledConfig.url, "/api/v1/developer_keys/test_id");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});

test("post_developer_keys_id_regenerate_secret calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = developer_keysModule.handlers.post_developer_keys_id_regenerate_secret;
  assert.ok(handler, "Handler post_developer_keys_id_regenerate_secret should be defined");

  const result = await handler(mockClient, {"id": "test_id"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/developer_keys/test_id/regenerate_secret");
  assert.deepStrictEqual(calledConfig.data, {});
  assert.deepStrictEqual(result, { success: true });
});
