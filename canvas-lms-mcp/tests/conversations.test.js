// Auto-generated tests for conversations
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const test = require("node:test");
const assert = require("node:assert");
const conversationsModule = require("../tools/conversations");

test("get_conversations calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = conversationsModule.handlers.get_conversations;
  assert.ok(handler, "Handler get_conversations should be defined");

  const result = await handler(mockClient, {"scope": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/conversations");
  assert.deepStrictEqual(calledConfig.params, {"scope": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("post_conversations calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = conversationsModule.handlers.post_conversations;
  assert.ok(handler, "Handler post_conversations should be defined");

  const result = await handler(mockClient, {"recipients[]": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/conversations");
  assert.deepStrictEqual(calledConfig.data, {"recipients[]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("get_conversations_batches calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = conversationsModule.handlers.get_conversations_batches;
  assert.ok(handler, "Handler get_conversations_batches should be defined");

  const result = await handler(mockClient, {});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/conversations/batches");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});

test("get_conversations_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = conversationsModule.handlers.get_conversations_id;
  assert.ok(handler, "Handler get_conversations_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "interleave_submissions": true});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/conversations/test_id");
  assert.deepStrictEqual(calledConfig.params, {"interleave_submissions": true});
  assert.deepStrictEqual(result, { success: true });
});

test("put_conversations_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = conversationsModule.handlers.put_conversations_id;
  assert.ok(handler, "Handler put_conversations_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "conversation[workflow_state]": "test_val"});

  assert.strictEqual(calledConfig.method, "put");
  assert.strictEqual(calledConfig.url, "/api/v1/conversations/test_id");
  assert.deepStrictEqual(calledConfig.data, {"conversation[workflow_state]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("post_conversations_mark_all_as_read calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = conversationsModule.handlers.post_conversations_mark_all_as_read;
  assert.ok(handler, "Handler post_conversations_mark_all_as_read should be defined");

  const result = await handler(mockClient, {});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/conversations/mark_all_as_read");
  assert.deepStrictEqual(calledConfig.data, {});
  assert.deepStrictEqual(result, { success: true });
});

test("delete_conversations_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = conversationsModule.handlers.delete_conversations_id;
  assert.ok(handler, "Handler delete_conversations_id should be defined");

  const result = await handler(mockClient, {"id": "test_id"});

  assert.strictEqual(calledConfig.method, "delete");
  assert.strictEqual(calledConfig.url, "/api/v1/conversations/test_id");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});

test("post_conversations_id_add_recipients calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = conversationsModule.handlers.post_conversations_id_add_recipients;
  assert.ok(handler, "Handler post_conversations_id_add_recipients should be defined");

  const result = await handler(mockClient, {"id": "test_id", "recipients[]": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/conversations/test_id/add_recipients");
  assert.deepStrictEqual(calledConfig.data, {"recipients[]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("post_conversations_id_add_message calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = conversationsModule.handlers.post_conversations_id_add_message;
  assert.ok(handler, "Handler post_conversations_id_add_message should be defined");

  const result = await handler(mockClient, {"id": "test_id", "body": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/conversations/test_id/add_message");
  assert.deepStrictEqual(calledConfig.data, {"body": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("post_conversations_id_remove_messages calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = conversationsModule.handlers.post_conversations_id_remove_messages;
  assert.ok(handler, "Handler post_conversations_id_remove_messages should be defined");

  const result = await handler(mockClient, {"id": "test_id", "remove[]": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/conversations/test_id/remove_messages");
  assert.deepStrictEqual(calledConfig.data, {"remove[]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("put_conversations calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = conversationsModule.handlers.put_conversations;
  assert.ok(handler, "Handler put_conversations should be defined");

  const result = await handler(mockClient, {"conversation_ids[]": "test_val"});

  assert.strictEqual(calledConfig.method, "put");
  assert.strictEqual(calledConfig.url, "/api/v1/conversations");
  assert.deepStrictEqual(calledConfig.data, {"conversation_ids[]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("get_conversations_find_recipients calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = conversationsModule.handlers.get_conversations_find_recipients;
  assert.ok(handler, "Handler get_conversations_find_recipients should be defined");

  const result = await handler(mockClient, {});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/conversations/find_recipients");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});

test("get_conversations_unread_count calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = conversationsModule.handlers.get_conversations_unread_count;
  assert.ok(handler, "Handler get_conversations_unread_count should be defined");

  const result = await handler(mockClient, {});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/conversations/unread_count");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});
