// Auto-generated tests for discovery_pages
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const test = require("node:test");
const assert = require("node:assert");
const discovery_pagesModule = require("../tools/discovery_pages");

test("get_discovery_pages calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = discovery_pagesModule.handlers.get_discovery_pages;
  assert.ok(handler, "Handler get_discovery_pages should be defined");

  const result = await handler(mockClient, {});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/discovery_pages");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});

test("put_discovery_pages calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = discovery_pagesModule.handlers.put_discovery_pages;
  assert.ok(handler, "Handler put_discovery_pages should be defined");

  const result = await handler(mockClient, {"discovery_page[primary][][authentication_provider_id]": 123});

  assert.strictEqual(calledConfig.method, "put");
  assert.strictEqual(calledConfig.url, "/api/v1/discovery_pages");
  assert.deepStrictEqual(calledConfig.data, {"discovery_page[primary][][authentication_provider_id]": 123});
  assert.deepStrictEqual(result, { success: true });
});

test("post_discovery_pages_token calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = discovery_pagesModule.handlers.post_discovery_pages_token;
  assert.ok(handler, "Handler post_discovery_pages_token should be defined");

  const result = await handler(mockClient, {"discovery_page[primary][][authentication_provider_id]": 123});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/discovery_pages/token");
  assert.deepStrictEqual(calledConfig.data, {"discovery_page[primary][][authentication_provider_id]": 123});
  assert.deepStrictEqual(result, { success: true });
});
