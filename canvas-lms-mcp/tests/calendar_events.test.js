// Auto-generated tests for calendar_events
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const test = require("node:test");
const assert = require("node:assert");
const calendar_eventsModule = require("../tools/calendar_events");

test("get_calendar_events calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = calendar_eventsModule.handlers.get_calendar_events;
  assert.ok(handler, "Handler get_calendar_events should be defined");

  const result = await handler(mockClient, {"type": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/calendar_events");
  assert.deepStrictEqual(calledConfig.params, {"type": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("get_users_user_id_calendar_events calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = calendar_eventsModule.handlers.get_users_user_id_calendar_events;
  assert.ok(handler, "Handler get_users_user_id_calendar_events should be defined");

  const result = await handler(mockClient, {"user_id": "test_user_id", "type": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/users/test_user_id/calendar_events");
  assert.deepStrictEqual(calledConfig.params, {"type": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("post_calendar_events calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = calendar_eventsModule.handlers.post_calendar_events;
  assert.ok(handler, "Handler post_calendar_events should be defined");

  const result = await handler(mockClient, {"calendar_event[context_code]": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/calendar_events");
  assert.deepStrictEqual(calledConfig.data, {"calendar_event[context_code]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("get_calendar_events_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = calendar_eventsModule.handlers.get_calendar_events_id;
  assert.ok(handler, "Handler get_calendar_events_id should be defined");

  const result = await handler(mockClient, {"id": "test_id"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/calendar_events/test_id");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});

test("post_calendar_events_id_reservations calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = calendar_eventsModule.handlers.post_calendar_events_id_reservations;
  assert.ok(handler, "Handler post_calendar_events_id_reservations should be defined");

  const result = await handler(mockClient, {"id": "test_id"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/calendar_events/test_id/reservations");
  assert.deepStrictEqual(calledConfig.data, {});
  assert.deepStrictEqual(result, { success: true });
});

test("post_calendar_events_id_reservations_participant_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = calendar_eventsModule.handlers.post_calendar_events_id_reservations_participant_id;
  assert.ok(handler, "Handler post_calendar_events_id_reservations_participant_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "participant_id": "test_participant_id", "comments": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/calendar_events/test_id/reservations/test_participant_id");
  assert.deepStrictEqual(calledConfig.data, {"comments": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("put_calendar_events_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = calendar_eventsModule.handlers.put_calendar_events_id;
  assert.ok(handler, "Handler put_calendar_events_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "calendar_event[context_code]": "test_val"});

  assert.strictEqual(calledConfig.method, "put");
  assert.strictEqual(calledConfig.url, "/api/v1/calendar_events/test_id");
  assert.deepStrictEqual(calledConfig.data, {"calendar_event[context_code]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("delete_calendar_events_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = calendar_eventsModule.handlers.delete_calendar_events_id;
  assert.ok(handler, "Handler delete_calendar_events_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "cancel_reason": "test_val"});

  assert.strictEqual(calledConfig.method, "delete");
  assert.strictEqual(calledConfig.url, "/api/v1/calendar_events/test_id");
  assert.deepStrictEqual(calledConfig.params, {"cancel_reason": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("post_calendar_events_save_enabled_account_calendars calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = calendar_eventsModule.handlers.post_calendar_events_save_enabled_account_calendars;
  assert.ok(handler, "Handler post_calendar_events_save_enabled_account_calendars should be defined");

  const result = await handler(mockClient, {"mark_feature_as_seen": true});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/calendar_events/save_enabled_account_calendars");
  assert.deepStrictEqual(calledConfig.data, {"mark_feature_as_seen": true});
  assert.deepStrictEqual(result, { success: true });
});

test("post_courses_course_id_calendar_events_timetable calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = calendar_eventsModule.handlers.post_courses_course_id_calendar_events_timetable;
  assert.ok(handler, "Handler post_courses_course_id_calendar_events_timetable should be defined");

  const result = await handler(mockClient, {"course_id": "test_course_id", "timetables[course_section_id][]": ["test_val"]});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/courses/test_course_id/calendar_events/timetable");
  assert.deepStrictEqual(calledConfig.data, {"timetables[course_section_id][]": ["test_val"]});
  assert.deepStrictEqual(result, { success: true });
});

test("get_courses_course_id_calendar_events_timetable calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = calendar_eventsModule.handlers.get_courses_course_id_calendar_events_timetable;
  assert.ok(handler, "Handler get_courses_course_id_calendar_events_timetable should be defined");

  const result = await handler(mockClient, {"course_id": "test_course_id"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/courses/test_course_id/calendar_events/timetable");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});

test("post_courses_course_id_calendar_events_timetable_events calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = calendar_eventsModule.handlers.post_courses_course_id_calendar_events_timetable_events;
  assert.ok(handler, "Handler post_courses_course_id_calendar_events_timetable_events should be defined");

  const result = await handler(mockClient, {"course_id": "test_course_id", "course_section_id": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/courses/test_course_id/calendar_events/timetable_events");
  assert.deepStrictEqual(calledConfig.data, {"course_section_id": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});
