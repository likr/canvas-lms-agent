// Auto-generated tests for appointment_groups
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const test = require("node:test");
const assert = require("node:assert");
const appointment_groupsModule = require("../tools/appointment_groups");

test("get_appointment_groups calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = appointment_groupsModule.handlers.get_appointment_groups;
  assert.ok(handler, "Handler get_appointment_groups should be defined");

  const result = await handler(mockClient, {"scope": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/appointment_groups");
  assert.deepStrictEqual(calledConfig.params, {"scope": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("post_appointment_groups calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = appointment_groupsModule.handlers.post_appointment_groups;
  assert.ok(handler, "Handler post_appointment_groups should be defined");

  const result = await handler(mockClient, {"appointment_group[context_codes][]": "test_val"});

  assert.strictEqual(calledConfig.method, "post");
  assert.strictEqual(calledConfig.url, "/api/v1/appointment_groups");
  assert.deepStrictEqual(calledConfig.data, {"appointment_group[context_codes][]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("get_appointment_groups_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = appointment_groupsModule.handlers.get_appointment_groups_id;
  assert.ok(handler, "Handler get_appointment_groups_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "include[]": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/appointment_groups/test_id");
  assert.deepStrictEqual(calledConfig.params, {"include[]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("put_appointment_groups_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = appointment_groupsModule.handlers.put_appointment_groups_id;
  assert.ok(handler, "Handler put_appointment_groups_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "appointment_group[context_codes][]": "test_val"});

  assert.strictEqual(calledConfig.method, "put");
  assert.strictEqual(calledConfig.url, "/api/v1/appointment_groups/test_id");
  assert.deepStrictEqual(calledConfig.data, {"appointment_group[context_codes][]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("delete_appointment_groups_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = appointment_groupsModule.handlers.delete_appointment_groups_id;
  assert.ok(handler, "Handler delete_appointment_groups_id should be defined");

  const result = await handler(mockClient, {"id": "test_id", "cancel_reason": "test_val"});

  assert.strictEqual(calledConfig.method, "delete");
  assert.strictEqual(calledConfig.url, "/api/v1/appointment_groups/test_id");
  assert.deepStrictEqual(calledConfig.params, {"cancel_reason": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("get_appointment_groups_id_users calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = appointment_groupsModule.handlers.get_appointment_groups_id_users;
  assert.ok(handler, "Handler get_appointment_groups_id_users should be defined");

  const result = await handler(mockClient, {"id": "test_id", "registration_status": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/appointment_groups/test_id/users");
  assert.deepStrictEqual(calledConfig.params, {"registration_status": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("get_appointment_groups_id_groups calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = appointment_groupsModule.handlers.get_appointment_groups_id_groups;
  assert.ok(handler, "Handler get_appointment_groups_id_groups should be defined");

  const result = await handler(mockClient, {"id": "test_id", "registration_status": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/appointment_groups/test_id/groups");
  assert.deepStrictEqual(calledConfig.params, {"registration_status": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});

test("get_appointment_groups_next_appointment calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = appointment_groupsModule.handlers.get_appointment_groups_next_appointment;
  assert.ok(handler, "Handler get_appointment_groups_next_appointment should be defined");

  const result = await handler(mockClient, {"appointment_group_ids[]": "test_val"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/v1/appointment_groups/next_appointment");
  assert.deepStrictEqual(calledConfig.params, {"appointment_group_ids[]": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});
