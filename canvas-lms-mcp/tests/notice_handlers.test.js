// Auto-generated tests for notice_handlers
// Generated by generate_tools.py - DO NOT EDIT MANUALLY

const test = require("node:test");
const assert = require("node:assert");
const notice_handlersModule = require("../tools/notice_handlers");

test("get_notice_handlers_context_external_tool_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = notice_handlersModule.handlers.get_notice_handlers_context_external_tool_id;
  assert.ok(handler, "Handler get_notice_handlers_context_external_tool_id should be defined");

  const result = await handler(mockClient, {"context_external_tool_id": "test_context_external_tool_id"});

  assert.strictEqual(calledConfig.method, "get");
  assert.strictEqual(calledConfig.url, "/api/lti/notice-handlers/test_context_external_tool_id");
  assert.deepStrictEqual(calledConfig.params, {});
  assert.deepStrictEqual(result, { success: true });
});

test("put_notice_handlers_context_external_tool_id calls correct endpoint", async () => {
  let calledConfig = null;
  const mockClient = async (config) => {
    calledConfig = config;
    return { data: { success: true } };
  };

  const handler = notice_handlersModule.handlers.put_notice_handlers_context_external_tool_id;
  assert.ok(handler, "Handler put_notice_handlers_context_external_tool_id should be defined");

  const result = await handler(mockClient, {"context_external_tool_id": "test_context_external_tool_id", "notice_type": "test_val"});

  assert.strictEqual(calledConfig.method, "put");
  assert.strictEqual(calledConfig.url, "/api/lti/notice-handlers/test_context_external_tool_id");
  assert.deepStrictEqual(calledConfig.data, {"notice_type": "test_val"});
  assert.deepStrictEqual(result, { success: true });
});
